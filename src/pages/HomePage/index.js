import { CardComponent } from '../../Components/CardComponent';

const HomePage = (props) => {

    return (
      
        <CardComponent heading='Card' {...props}/>
        
    );

}

export default HomePage; 